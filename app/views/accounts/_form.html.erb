<%= form_for(account) do |f| %>
  <% if account.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(account.errors.count, "error") %> prohibited this account from being saved:</h2>

      <ul>
      <% account.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :has_farm_store_accounts_type %>
    <%= f.text_field :has_farm_store_accounts_type %>
  </div>

  <div class="field">
    <%= f.label :has_farm_store_accounts_id %>
    <%= f.number_field :has_farm_store_accounts_id %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
